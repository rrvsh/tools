# builders

## RULES:
- Each file should output or help to output one of the flake outputs.
- Any file that doesn't should be moved to options under a `default.nix`.
- Any logic that doesn't should heavily be considered moving to options under a `default.nix`.
