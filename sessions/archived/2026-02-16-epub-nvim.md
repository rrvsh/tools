# Session: Add epub.nvim to Neovim config

## Work breakdown
- Locate Neovim config and plugin management in nix modules.
- Retrieve epub.nvim installation and usage details from upstream README.
- Update Nix Neovim plugin list and Lua config to initialize epub.nvim.
- Provide usage guide for opening EPUBs and navigation.
- Update flake inputs/lockfile for epub.nvim source.
- Enable auto-open and add keymaps for all epub.nvim actions.

## Decisions
- Configure epub.nvim via `require("epub").setup()` to align with repo's Lua setup pattern.
- Add the plugin in `nix/modules/neovim.nix` to keep plugin management centralized.
- Track the plugin as a non-flake input in `flake.nix` to avoid manual hashes.
- Include `unzip` in Neovim extra packages to satisfy epub.nvim runtime requirements.
- Add leader keymaps that wrap epub.nvim defaults to make discovery easier.

## Learnings
- None yet.

## Bugs encountered
- None yet.
